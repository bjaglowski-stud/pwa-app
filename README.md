
  # Simple auction service

  This is a code bundle for Simple auction service. The original project is available at https://www.figma.com/design/Jadbc4d4zik0KQkEvlGIzW/Simple-auction-service.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  